// ============================================================
// App Root Component — Placeholder (v2-04)
// ============================================================
// Full implementation pending v2-04 (HTMLAdapter + Shadow DOM UI).
// This stub exists so the existing content/index.tsx entry
// compiles without errors as we build v2-02 and v2-03.

import React from "react";

export function App(): React.ReactElement {
  return React.createElement("div", { id: "brave-tts-ui-root" });
}
