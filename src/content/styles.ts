// ============================================================
// Style Injection — Placeholder (v2-04)
// ============================================================
// Full implementation pending v2-04 (Shadow DOM + Tailwind).
// This stub exists so the existing content/index.tsx entry
// compiles without errors as we build v2-02 and v2-03.

/** Inject component styles into a ShadowRoot. No-op stub. */
export function injectStyles(_shadow: ShadowRoot): void {
  // v2-04: Inject Tailwind CSS string here
}
